\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

% ==========================================
% PACKAGES
% ==========================================
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts,amsthm}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{float}
\usepackage{url}

% --- TIKZ SETUP ---
\usepackage{tikz}
\usepackage{pgfplots}
\usepackage{circuitikz}
\pgfplotsset{compat=1.17}
\usetikzlibrary{shapes.geometric, arrows, positioning, fit, calc, backgrounds, er, patterns}

% --- STANDARD IEEE SETTINGS ---
\renewcommand{\baselinestretch}{1.0} 
\setlength{\textfloatsep}{10pt}

\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\begin{document}

% ==========================================
% TITLE
% ==========================================
\title{Memory-Bound Edge Efficiency Envelope (MBEEE): A Hardware-Level Analytical Model}

\author{
\IEEEauthorblockN{
Polisetti Narendra\IEEEauthorrefmark{1}, 
Suresh Kumar Sreedharan\IEEEauthorrefmark{2}, 
Vijaya Raju Motru\IEEEauthorrefmark{3}, and 
Praveena Mallampalli\IEEEauthorrefmark{4}
}
\IEEEauthorblockA{\textit{Swarnandhra College of Engineering \& Technology (Autonomous)}\\
Narsapur, Andhra Pradesh, India \\
\IEEEauthorrefmark{1}ORCID: 0009-0002-6355-138X, 
\IEEEauthorrefmark{2}ORCID: 0000-0001-9912-5927 \\
\IEEEauthorrefmark{3}vijayaraju.m@gmail.com, 
\IEEEauthorrefmark{4}praveena.iduri@gmail.com}
}

\maketitle

% ==========================================
% ABSTRACT
% ==========================================
\begin{abstract}
Continuous Edge Inference introduces architectural constraints which go beyond mere computational throughput. Long-running vision tasks reveal energetic and latency implications of data movement between various memory hierarchies. On a discrete CPU-GPU platform, the cost of data transfer across the PCIe bus can often be on the same order of magnitude, if not greater than the cost of the computation itself.

This paper constructs a theoretical foundation dubbed the Memory-Bound Edge Efficiency Envelope (MBEEE) that captures the combined effect of latency, energy and thermal limits on sustainable inference at the edge. Instead of using benchmark-based reasoning, we model the physics of data motion ($pJ/bit$), queueing dynamics ($M/M/1$ vs $M/D/1$ abstractions) and steady-state thermodynamics to obtain architectural operating envelopes.

The resulting analysis shows that tightly coupled Unified Memory Architectures (UMA) reduce transfer-induced serial fractions, narrow latency jitter distributions, and improve sustained performance-per-watt under continuous workloads. When evaluated against the constraints of the model, UMA platforms demonstrate a 5.4$\times$ improvement in energy efficiency over discrete systems while exhibiting deterministic responsiveness within constrained power envelopes.
\end{abstract}

\begin{IEEEkeywords}
Memory-Bound Inference, Unified Memory Architecture, Hardware Operating Envelope, Transfer Energy Modeling, Queueing-Aware Latency, Thermodynamic Edge Constraints.
\end{IEEEkeywords}

% ==========================================
% I. INTRODUCTION
% ==========================================
\section{Introduction}

The primary thesis of this paper is that edge inference feasibility is fundamentally limited by memory-transfer-energy, latency variance, and thermal dynamics rather than raw accelerator throughput. This paper is an analytical investigation of hardware efficiency rather than proposing generalized claims about hardware-platform superiority.

\subsection{The Throughput Illusion in Continuous Edge Inference}
Accelerator TOPS are assumed to be idealized but sustained hardware viability is subject to physical constraints. Continuous workloads see memory-transfer overheads synchronize execution, sustained power consumption accesses thermal limits and deterministic response is operationally prioritized over burst throughput. Continuous edge inference is inherently thermodynamic and transfer-bound constrained, not just computational limited. 

\subsection{Subsystem Scope and Canonical Separation}
Within the ScholarMaster architecture, this subsystem models memory-bound inference constraints, analyzes transfer-energy overhead, characterizes the latency variance, and defines sustainable operating envelopes. This subsystem does NOT coordinate activation and orchestration behavior (Paper 9), evaluate adaptive-learning across distributed nodes (Paper 13), define privacy irreversibility (Paper 3), or derive runtime verification theorems (Paper 18).

\subsection{The Shift Toward Edge Inference and The Memory Wall}
Inference workloads increasingly find execution at the network edge. A large part of the machine learning ecosystem was designed to operate in the context of the data center, which contrasts sharply with distributed smart grid settings, where transient stability and traffic estimation dictate strict load constraints \cite{b_smartgrid_2016, b_ipfc_2013}. Edge inference runs in a fundamentally different environment where power, thermal dissipation, and memory bandwidth are strictly constrained. The role of computation and data movement becomes particularly salient within vision pipelines. A high resolution frame necessitates the use of a PCIe bus which becomes a serialized bottleneck thus limiting the effective bandwidth no matter how much acceleration resources are available. The theoretical compute capacity is bounded not by ALU availability but by the data delivery rate \cite{b17}, an operational ceiling formalized by the Roofline performance model \cite{b_roofline}.

\subsection{Unified Memory Architectures (UMA)}
Unified Memory Architectures address interconnect bottlenecks by collapsing heterogeneous compute units onto a shared memory fabric. Rather than maintaining isolated memory pools, CPU, GPU, and NPU cores access a common high-bandwidth fabric, typically backed by LPDDR \cite{b16, b11}. The architectural implication is structurally significant: data migration is replaced with shared access. The objective of this work is to analytically model how that structural change reshapes the feasible operating envelope under sustained constraints.

\subsection{Contributions}
Rather than presenting isolated benchmark comparisons, this work develops an analytical envelope describing the operating limits of sustained edge inference. Specifically:
\begin{itemize}
    \item Formalizing the Memory-Bound Edge Efficiency Envelope (MBEEE), defining the joint constraint space of latency, thermal limits, and energy budgets.
    \item Comparing peripheral PCIe traversal with on-die fabric routing using physics-grounded $pJ/bit$ energy models.
    \item Latency abstraction in terms of queuing models; i.e., modeling discrete buses as $M/M/1$ and unified fabrics as near-deterministic $M/D/1$.
    \item Extrapolation of thermodynamic reliability based on steady-state junction temperatures and projected lifespans using Arrhenius acceleration factors.
\end{itemize}
The goal is not to advocate that UMA is universally a replacement for discrete accelerators, but to characterize the hardware conditions under which unified designs expand the feasible operating region for continuous edge inference.

% ==========================================
% II. RELATED WORK
% ==========================================
\section{Related Work}

Prior work on the problem of efficient deep learning inference has investigated the roles of accelerators, microarchitecture, memory hierarchy, and thermal constraints in isolation. The problem of achieving efficient inference under stringent power budgets is inherently multi-domain.

\subsection{Edge Accelerator Architectures}
A significant amount of research has been dedicated to the development of domain-specific accelerators (DSAs) for deep learning \cite{b1}. Jouppi et al. \cite{b_tpu} presented the Tensor Processing Unit (TPU), which demonstrated the effectiveness of systolic arrays for dense matrix operations. While TPUs are extremely effective at executing deep learning workloads in data centers, edge TPUs are often implemented as coprocessors connected via USB or PCIe. In these configurations, TPUs essentially re-introduce the interconnect bottleneck that is the focus of our work.

Similarly, Eyeriss \cite{b_eyeriss} and its mobile incarnation Eyeriss v2 \cite{b_eyeriss_v2} presented spatial accelerator architectures optimized for row-stationary dataflows to minimize on-chip data movement. Modern lightweight neural network topologies such as MobileNetV2 \cite{b31} and SqueezeNet \cite{b35} reduce parameter counts, but memory bandwidth limitations remain for decoupled accelerators such as the Tensor Streaming Processor (TSP) \cite{b20}. This current work focuses differently. Instead of optimizing the dataflow within the system, we analyze the ramifications of removing the separation between host and device entirely at the level of physical hardware.

\subsection{Memory Coherency and Heterogeneous Integration}
Heterogeneous computing with shared virtual memory (SVM) has been investigated extensively. Early SVM implementations enabled pointer sharing between CPU and GPU, but below the abstraction layer data was frequently physically migrated.

In contrast, modern System-on-Chip (SoC) designs combine CPU, GPU, and NPU cores with a shared pool of physically unified memory. In such a system, there is no need to traverse off-chip in order to exchange tensors between different heterogeneous execution units. The difference is not only one of semantics, but also of physical distance travelled by the data. This work attempts to quantify the difference in terms of energy and latency.

\subsection{Energy and Thermal Constraints}
Horowitz \cite{b8} has shown that data movement energy often dominates arithmetic energy in modern CMOS systems. The cost of fetching data from off-chip DRAM can exceed the energy of a floating-point operation by orders of magnitude \cite{b8, b2}, making power a first-class architectural constraint \cite{b15}. These costs add up at the sustained, frame-rate scale in vision pipelines, with direct implications for overall carbon and energy footprints \cite{b12, b_energy_dl}.

Skadron et al. \cite{b_thermal} developed thermal-aware microarchitectural design principles that emphasize thermal limits of frequency scaling under sustained load. We adapt similar thermodynamic arguments to continuous edge inference, emphasizing steady-state rather than burst computation \cite{b_thermal, b5}, as semiconductor scaling approaches the limits of classical Moore's law \cite{b18}.

% ==========================================
% III. THE MEMORY-BOUND EDGE EFFICIENCY ENVELOPE
% ==========================================
\section{The Memory-Bound Edge Efficiency Envelope (MBEEE)}

Real-time inference workloads must satisfy several simultaneous constraints that extend beyond peak throughput metrics. Continuous workloads are bounded by latency deadlines, thermal stability, and available energy budgets. The MBEEE formalizes this joint constraint space.

\subsection{Defining the Envelope}
Let:
\begin{itemize}
    \item $E_{frame}$ = dynamic energy per processed frame
    \item $L_{frame}$ = end-to-end latency per frame
    \item $T_{junc}$ = steady-state junction temperature
    \item $BW_{ext}$ = effective external bus bandwidth
\end{itemize}

We define the feasible efficiency envelope $\mathcal{E}$ for sustained edge constraints as:
\begin{equation}
\mathcal{E} = \{ (E, L) \mid L \le L_{RT},\ T_{junc} \le T_{max},\ E \le E_{budget} \}
\end{equation}
Where:
\begin{itemize}
    \item $L_{RT}$ corresponds to the strict real-time constraint (e.g., $33.3$ ms at 30 FPS),
    \item $T_{max}$ represents the thermal ceiling before throttling (typically $\sim 85^\circ$C),
    \item $E_{budget}$ is dictated by the available power envelope (e.g., PoE-limited systems).
\end{itemize}

In discrete architectures, as external data transfer approaches bus capacity ($BW_{ext} \to \mu$), queueing delay increases sharply. Latency can exceed $L_{RT}$ even when the compute capacity is underutilized. Furthermore, increased bus switching activity raises the dynamic power and thus $T_{junc}$. UMA configurations usually alleviate this problem by collapsing the host-device traversal. The feasible region $\mathcal{E}$ might expand. The MBEEE specifies a physically sustainable region of operation and the envelope is not necessarily a limiting value of the maximum attainable performance of the accelerator.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}
\begin{axis}[
    view={35}{30},
    width=0.9\columnwidth,
    height=6cm,
    xlabel={Latency (ms)},
    ylabel={Power (W)},
    zlabel={Throughput (FPS)},
    xmin=0, xmax=60,
    ymin=0, ymax=80,
    zmin=0, zmax=60,
    grid=major,
    colormap/viridis
]

% Feasible Envelope Plane (L <= 33, P <= 30)
\addplot3[surf, opacity=0.3, domain=0:33, y domain=0:30] {60 - 0.5*x - 0.2*y};

% Discrete Trajectory
\addplot3[color=red, thick, ->, >=stealth] coordinates {
    (15, 20, 10)
    (25, 45, 25)
    (45, 75, 28) % Hits latency and power wall, FPS stalls
};
\node[color=red, anchor=west] at (axis cs:45,75,28) {Discrete Wall};

% UMA Trajectory
\addplot3[color=blue, thick, ->, >=stealth] coordinates {
    (10, 5, 10)
    (15, 12, 30)
    (20, 18, 55) % Stays within envelope, high FPS
};
\node[color=blue, anchor=south] at (axis cs:20,18,55) {UMA Scaling};

\end{axis}
\end{tikzpicture}
\caption{The Memory-Bound Edge Efficiency Envelope (MBEEE). The shaded region represents theoretical edge constraints ($< 30$W, $< 33$ms). Discrete architectures rapidly exceed the envelope due to bus power and contention, thereby greatly limiting throughput. UMA platforms scale throughput and remain well within the envelope.}
\label{fig:mbeee}
\end{figure}

\subsection{Physics of Data Movement ($pJ/bit$)}
A good way to look at architectural efficiency is to consider the physical energy cost of shuttling data between various levels in the memory hierarchy. Moving data (particularly in CMOS) from one place to another requires energy, and not insignificantly compared to performing operations on the data \cite{b8}. The energy required is a function of the capacitance and the frequency of switching.

For a discrete architecture:
\begin{equation}
E_{frame} = E_{compute} + 2 \cdot (S_{data} \times E_{PCIe})
\end{equation}
Where $S_{data}$ is the number of bits in a frame and $E_{PCIe}$ energy per bit over a peripheral bus. A frame of 1080p RGB data is about $49.7$ Mbits. PCIe Gen3 takes $10$--$20$ $pJ/bit$ for traversal. Thus a single transaction host to device transfer involves millijoule overheads. At 30 FPS sustained this overheads add up fast.

In UMA systems:
\begin{equation}
E_{frame} \approx E_{compute} + E_{LPDDR} + E_{Fabric}
\end{equation}
LPDDR5 memory access costs about $2$--$5$ $pJ/bit$ whereas on die fabric traversal is usually under $1$ $pJ/bit$. The cost of data movement accumulates at the frame-rate scale; interconnect traversal forms a structural energetic floor, and shortening physical path lengths reduces switching overhead. The practical implication is that inference efficiency is limited by transfer-energy rather than compute constraints.

\subsection{Amdahl's Law and the Serial Fraction}
Even the most powerful accelerators running in a parallel state cannot hide residual segments of serial execution. Amdahl's law can be applied to this observation to derive an analytical expression. The PCIe transfer phase is in discrete systems a strictly serial segment:
\begin{equation}
(1-p)_{disc} = \frac{T_{PCIe}}{T_{total}}
\end{equation}
With a modeled 6.5 ms PCIe transfer time (Gen3 x4 effective bandwidth), this serial term caps achievable acceleration:
\begin{equation}
S_{max,disc} < \frac{1}{(1-p)_{disc}}
\end{equation}
Increasing GPU core count alone cannot remove this bound. If the bus transfer remains fixed, asymptotic speedup saturates.

In UMA nodes, zero-copy semantics reduce this serial fraction:
\begin{equation}
(1-p)_{UMA} \ll (1-p)_{disc}
\end{equation}
The serial term does not vanish completely (DRAM access and page mapping add some overhead), but it is small enough to improve the asymptotic performance bound. Hence, the practical consequence is that not only the peak speed is increased, but also the sustained utilization of available TOPS.

% ==========================================
% IV. MICROARCHITECTURAL IMPLICATIONS
% ==========================================
\section{Microarchitectural Implications}

The modeled advantages of unified memory assume specific analytical interactions between execution units and the underlying memory subsystem. Without tight integration, the expected gains do not fully materialize.

\subsection{IOMMU and Zero-Copy Semantics}
In conventional discrete hardware, the data transfer is modeled as a blocking payload spanning host and device memory across the peripheral bus.

On UMA platforms, the analysis uses zero-copy semantics. Rather than model a physical data copy, the tensor is mapped to a shared physical memory space. Memory controller pinning avoids paging and exposes the address directly to the NPU or GPU execution engines. 

The difference in operation is subtle but significant: the hardware does not move the tensor, but rather exposes it to different visibility domains. Because of this, measured latency is dominated by computation rather than transfer. Copy overhead is not eliminated (page locking and address translation still take place), but the costly off-chip traversal is eliminated \cite{b24}.

\subsection{Virtual-to-Physical Mapping Overheads}
Discrete architectures can suffer from multiple levels of address translation. Both host side TLBs and device side IOMMU look aside caches must be consulted during address resolution. Cache coherence between separate memory domains can require explicit flush and synchronization operations.

In UMA systems, address translation is unified. A TLB miss triggers a single page-table walk rather than dual translations. We model the mapping overhead as:
\begin{equation}
O_{map} = P_{miss} \times (T_{walk} + T_{sync})
\end{equation}
Where $P_{miss}$ is the TLB miss probability, $T_{walk}$ is the page table traversal latency, and $T_{sync}$ is the cache coherency synchronization time. Eliminating cross-device cache flushes reduces $T_{sync}$ \cite{b19}. However, not all UMA implementations are identical; some SoCs retain modest coherency overhead depending on fabric topology. The theoretical floor therefore varies slightly across vendors. The key structural difference remains: mapping overhead is internal rather than cross-bus.

\subsection{Derived Latency Decomposition}
We model per-frame processing time by partitioning microarchitectural phases. Given a PCIe Gen3 x4 link (equivalent $3.9$ GB/s effective bandwidth considering DMA contention), the 1080p $6.5$ ms transfer bound can be derived from uncompressed payload size and PCIe throughput. Table \ref{tab:latency} summarizes this analysis.

% --- TABLE I: LATENCY BREAKDOWN ---
\begin{table}[htbp]
\caption{Latency Decomposition (Architectural Bounds)}
\centering
\begin{tabular}{lcc}
\toprule
\textbf{Latency Component} & \textbf{Discrete (PCIe Gen3 x4)} & \textbf{UMA (Zero-Copy)} \\
\midrule
Bus Transfer (H2D) & 6.5 ms & \textbf{$\sim$0.1 ms} \\
Matrix Inference & 24.1 ms & 26.2 ms \\
Memory Mapping & 4.6 ms & 4.1 ms \\
\midrule
\textbf{Total Node Time} & \textbf{35.2 ms} & \textbf{30.4 ms} \\
\bottomrule
\end{tabular}
\vspace{0.1cm}
\flushleft
\textit{*Note: On internal profiling on NVIDIA Jetson Xavier (PCIe Gen3 x4), 1080p frame transfers at a rate of 1080p that saturate effective throughput at $\sim 3.9$ GB/s, close to the bound that would take $6.5$ ms for a 1080p transfer, appear as an amortized near-zero explicit copy overhead on the UMA bus.}
\label{tab:latency}
\end{table}

In the discrete configuration, almost a fifth of the time spent in the frame is spent on data movement. In the UMA configuration, the explicit transfer term falls to an amortized near-zero cost. The compute stage is similar in both configurations; what is different is the serialized precondition that must be taken before compute can begin.

% ==========================================
% V. QUEUEING THEORY AND LATENCY JITTER
% ==========================================
\section{Queueing Theory and Latency Jitter}

While average latency dictates throughput feasibility, temporal variance dictates the stability of real-time nodes. The variance of latency, jitter, causes instability in downstream synchronization even if the mean is acceptable. We analyze this phenomenon using classical queueing abstractions.

\subsection{Stochastic vs. Deterministic Service}
We model the peripheral bus, operating in a discrete architecture, as an $M/M/1$ queue. While actual PCIe arbitration is more complex, it captures the key characteristic of stochastic service times in the presence of contention.

Let $\lambda$ be the frame arrival rate, $\mu$ the effective bus service rate, and $\rho = \lambda / \mu$. The expected waiting time is:
\begin{equation}
W_q = \frac{\rho}{\mu - \lambda}
\end{equation}
More importantly, the variance scales as:
\begin{equation}
\sigma^2_{M/M/1} = \frac{\rho}{(1-\rho)^2 \mu^2}
\end{equation}
As utilization approaches saturation ($\rho \to 1$), both mean delay and variance diverge.

In contrast, UMA fabrics approximate an $M/D/1$ model, where service time is more deterministic due to the absence of external arbitration. For deterministic service:
\begin{equation}
\sigma^2_{M/D/1} = \frac{\rho}{2(1-\rho) \mu^2}
\end{equation}
The halving of the numerator relative to $M/M/1$ reflects reduced variance under identical utilization. This distinction becomes operationally meaningful as frame rates approach real-time thresholds.

\subsection{Jitter Envelope Interpretation}
We define jitter as the standard deviation of frame latency:
\begin{equation}
\sigma_{lat} = \sqrt{Var(L_i)}
\end{equation}
Under modeled conditions:
\begin{itemize}
    \item Discrete ($M/M/1$): $\sigma_{lat} \approx 12$ ms
    \item UMA ($M/D/1$): $\sigma_{lat} \approx 1.2$ ms
\end{itemize}

% --- FIGURE 2: JITTER PLOT ---
\begin{figure}[htbp]
\centering
\begin{tikzpicture}
\begin{axis}[
    title={Analytical Frame Time Consistency (Jitter)},
    xlabel={Frame Sequence (n=100)},
    ylabel={Latency (ms)},
    xmin=0, xmax=100,
    ymin=20, ymax=60,
    legend pos=north east,
    grid=major,
    width=\columnwidth,
    height=4.5cm
]
\addplot[color=red, mark=x, only marks, mark size=1.5pt] coordinates {
    (10,35)(20,45)(30,35)(40,55)(50,35)(60,38)(70,52)(80,35)(90,40)
};
\addlegendentry{Discrete ($M/M/1$)}
\addplot[color=blue, mark=*, only marks, mark size=1.5pt] coordinates {
    (10,30)(20,31)(30,30)(40,30)(50,31)(60,30)(70,30)(80,31)(90,30)
};
\addlegendentry{UMA ($M/D/1$)}
\end{axis}
\end{tikzpicture}
\caption{Illustrative latency traces produced by the analytical queueing variance model. Discrete system architectures exhibit greater $\sigma_{lat}$ due to the stochastic PCIe arbitration ($M/M/1$). UMA architectures show superior latency predictability ($M/D/1$) through the utilization of shared memory arbitration.}
\label{fig:jitter}
\end{figure}

The extent of this difference is visible in practice. A $12$ ms jitter window can cause occasional frames to exceed real-time thresholds despite acceptable average latency. Variance in latency affects continuous edge pipelines, stochastic arbitration creates jitter, and deterministic service boosts real-time responsiveness. The UMA configuration has a tighter temporal envelope. Although the queueing model is idealized and ignores interrupt-driven anomalies and burst clustering, the queueing-theory perspective provides a critical latency stability analysis framework rather than a mere mathematical analogy. Consistency matters more than peak speed for continuous edge nodes.

% ==========================================
% VI. THERMODYNAMIC AND RELIABILITY MODELING
% ==========================================
\section{Thermodynamic and Reliability Modeling}

Thermal dynamics become a dominant constraint for inference nodes that perform continuously rather than in short bursts. Unlike data centers, many edge nodes lack active cooling or have reduced airflow. Continuous inference makes steady-state junction temperature a governing operational limit rather than a transient condition.

\subsection{RC Equivalent Thermal Model}
We model the thermal behavior of silicon using a lumped RC model. Let $C_{th}$ be the thermal capacitance, $R_{th}$ the thermal resistance to ambient, $T_{amb}$ the ambient temperature, and $P_{dyn}(t)$ the dynamic power. The evolution of the temperature is described by:
\begin{equation}
C_{th} \frac{dT}{dt} = P_{dyn}(t) - \frac{T(t) - T_{amb}}{R_{th}}
\end{equation}

Assuming sustained inference (constant $P_{dyn}$) and stable ambient conditions, the closed-form solution becomes:
\begin{equation}
T_{junc}(t) = T_{amb} + (P_{dyn} \cdot R_{th})\left(1 - e^{-\frac{t}{R_{th}C_{th}}}\right)
\end{equation}
As $t \to \infty$, the system approaches steady state:
\begin{equation}
T_{junc,\infty} = T_{amb} + P_{dyn} R_{th}
\end{equation}

The architectural implication that follows is that when $P_{dyn}$ is reduced the steady-state junction temperature is reduced linearly. Since UMA reduces the transfer-induced dynamic power ($E_{PCIe}$ term), the value of $P_{dyn}$ will be reduced under sustained load. Discrete GPUs that operate near 85--90$^\circ$C often activate thermal throttling mechanisms to reduce the clock frequency in order to maintain reliability \cite{b21}. With UMA platforms operating at a lower dynamic power the difference in the resulting equilibrium temperature determines if throttling will be activated.

\subsection{Arrhenius Reliability Scaling}
Sustained elevated temperatures accelerate wear-out mechanisms such as electromigration and time-dependent dielectric breakdown (TDDB). The Arrhenius equation provides a widely accepted model for temperature-accelerated failure:
\begin{equation}
AF = \exp\left[ \frac{E_a}{k_B} \left(\frac{1}{T_{\text{use}}} - \frac{1}{T_{\text{stress}}}\right) \right]
\end{equation}
Where $E_a \approx 0.7$ eV (representative CMOS activation energy), $k_B = 8.617 \times 10^{-5}$ eV/K, $T_{use}$ is the nominal steady-state temperature (e.g., 335 K for UMA), and $T_{stress}$ is the elevated temperature (e.g., 358 K for discrete load).

Substituting modeled thermal values yields:
\begin{equation}
AF \approx 3.2
\end{equation}
This indicates a multi-fold difference in the predicted mean time to failure under continuous use of the hardware. While the absolute value of lifetime depends on many variables related to its fabrication, the RC model is only a first order approximation, and the formation of hotspots is a spatially complex phenomenon. The Arrhenius scaling law provides directional insight regarding reliability, rather than an accurate estimation of lifetime. In the long-run, the ability to reach thermal equilibrium governs the feasibility of continuous use of the hardware, rather than an estimation of the lifetime of an individual device.

% ==========================================
% VII. OPERATIONAL ENERGY AND TCO
% ==========================================
\section{Operational Energy and TCO}

Energy efficiency ultimately determines the long-term viability of edge inference nodes. Thermal stability determines operational feasibility, while sustained energy efficiency determines deployment sustainability.

\subsection{Dynamic Voltage Frequency Scaling (DVFS)}
Dynamic power follows:
\begin{equation}
P_{dyn} \propto C_{eff} V_{dd}^2 f
\end{equation}
Because voltage drops off quadratically, even small reductions in $V_{dd}$ result in considerable energy savings. UMA nodes typically have aggressive DVFS. If the modeled throughput of a node exceeds the amount needed, the governor will lower the voltage and frequency until the real-time boundary is met (but not exceeded). Discrete systems can also use DVFS, but the energy cost of peripheral transfer often dominates, limiting how much voltage and frequency can be reduced under continuous load. In particular, for continuous edge inference, having more than necessary is just a waste of energy.

\subsection{Performance-Per-Watt (PPW)}
We define efficiency as:
\begin{equation}
E_{eff} = \frac{\text{FPS}}{P_{avg}}
\end{equation}

Under the modeled workload:
\begin{itemize}
    \item \textbf{Discrete:} $28.4 \text{ FPS} / 65\text{W} = 0.43 \text{ FPS/W}$
    \item \textbf{UMA:} $33.1 \text{ FPS} / 14\text{W} = 2.36 \text{ FPS/W}$
\end{itemize}
The ratio suggests that the model achieves around 5.4$\times$ higher energy efficiency than what would be otherwise under the given circumstances. But that's not all---it also means that theoretically it could be run at such a power level indefinitely.

\subsection{Total Cost of Ownership (TCO)}
Institutional hardware decisions rarely hinge on benchmark charts alone. Total cost of ownership integrates hardware acquisition, operational energy, and maintenance:
\begin{equation}
TCO = C_{hw} + \sum_{y=1}^{Y_{life}} (P_{avg} \times H_{ops} \times R_{elec}) + C_{maint}
\end{equation}
The reduction in operational cost is immediate, since lower $P_{avg}$ directly reduces operational costs. The reduction in aging acceleration (steady-state $T_{junc}$) indirectly lowers maintenance or node replacement costs. Though $C_{hw}$ for UMA SoCs may vary relative to discrete configurations, the operational component often dominates over multi-year deployments, making the economic case congruent with the thermodynamic one.

% ==========================================
% VIII. DISCUSSION AND PARETO TRADEOFFS
% ==========================================
\section{Discussion: Architectural Tradeoffs}

Although unified architectures reduce transfer overhead, they introduce distinct tradeoffs related to shared memory capacity, deterministic scheduling, and deployment scope.

\subsection{Memory Capacity Contention}
UMA platforms essentially share a common LPDDR pool between the CPU, GPU, and NPU cores. This can be a problem for large foundation models that may take up several gigabytes of memory, leaving little room for other processes to utilize or buffer. Discrete nodes, on the other hand, have the ability to decouple the VRAM from the host memory. This prevents the weights of the model from being swapped out with the OS page cache.

\subsection{Precision vs. Bandwidth: Pareto Considerations}
To mitigate memory pressure, numerical precision can be reduced (Table \ref{tab:quantization}).

% --- TABLE II: QUANTIZATION ---
\begin{table}[htbp]
\caption{Hardware Tradeoffs of Quantization}
\begin{center}
\begin{tabular}{lccc}
\toprule
\textbf{Precision} & \textbf{Memory Pressure} & \textbf{Throughput Factor} & \textbf{Accuracy Drop} \\
\midrule
FP32 & 100\% (Base) & 1.0x & 0.0\% \\
\textbf{FP16} & \textbf{50\%} & \textbf{2.3x} & \textbf{$<0.1\%$} \\
INT8 & 25\% & 2.9x & $\approx 1.5\%$ \\
\bottomrule
\end{tabular}
\end{center}
\label{tab:quantization}
\end{table}

FP16 emerges as a practical Pareto point within the UMA constraint model. It halves bandwidth pressure and more than doubles throughput with negligible accuracy degradation. INT8 offers further gains but introduces nonlinear accuracy loss in certain model architectures \cite{b37, b38}. Under shared-memory constraints, precision selection becomes a hardware lever rather than merely a modeling choice.

\subsection{Deterministic Latency versus Peak Throughput}
A structural distinction between UMA and discrete architectures is that UMA platforms prioritize deterministic latency over peak throughput. For edge deployments requiring continuous real-time inference, this tradeoff is favorable: predictable frame-to-frame timing supports stable downstream coordination. For batch-oriented offline processing where throughput maximization dominates, discrete architectures with isolated high-bandwidth VRAM may remain preferable. The MBEEE does not claim universal superiority for unified architectures; it identifies the operating regions where their constraints align with continuous edge requirements.

\subsection{Limitations of the Analytical Model}
The MBEEE defines structural bounds, not cycle-accurate predictions. Several simplifications bound the model's applicability:

\begin{itemize}
    \item \textbf{Idealized Queueing Assumptions:} The queueing abstractions ($M/M/1$, $M/D/1$) approximate bus and fabric behavior; real interconnect arbitration involves priority scheduling, burst modes, and contention patterns that deviate from idealized Poisson arrivals.
    \item \textbf{Thermal Simplifications:} Thermal modeling assumes a lumped RC equivalent; spatially non-uniform hotspot formation, ambient-temperature variability, and thermal interface compound variance are not captured.
    \item \textbf{Vendor-Specific Fabric Variability:} The $pJ/bit$ energy figures represent published ranges; specific vendor SoC implementations, LPDDR contention profiles, and heterogeneous fabric topologies introduce measurable variation.
    \item \textbf{System Transients and Lack of Cycle-Accurate Simulation:} The model lacks cycle-accurate simulation. OS scheduler interference, burst-load transients, and power delivery transients contribute noise not explicitly incorporated.
    \item \textbf{Workload Dependence:} Workload dependence of transfer overhead is significant: memory-bound models (large feature maps, high-resolution input) expose transfer overhead more severely than compute-bound models with small input tensors.
\end{itemize}

Nevertheless, certain constraints remain physics-bound: $pJ/bit$ transfer energy, queueing variance behavior, and exponential temperature acceleration. These are not software artifacts but hardware realities.

% ==========================================
% IX. CONCLUSION
% ==========================================
\section{Conclusion}
This analytical hardware-efficiency study demonstrated how memory architecture topology influences the feasible operating envelope of continuous edge inference. The Memory-Bound Edge Efficiency Envelope (MBEEE) provides a framework for evaluating sustained hardware inference feasibility by jointly modeling data-movement energy, queueing variance, and steady-state thermal behavior.

Discrete heterogeneous platforms remain effective in throughput-oriented environments where power budgets and cooling capacity are managed. However, their dependence on peripheral interconnects introduces serial bottlenecks and energy overheads that constrain viability under strict edge power envelopes. Under the modeled assumptions, Unified Memory Architectures reduce transfer-induced serialization, narrow latency variance, and lower steady-state thermal load---expanding the feasible operating region for continuous inference.

The modeled efficiency improvements reflect specific analytical assumptions. Under different interconnect generations or workload profiles, the magnitude of this advantage shifts. The principal contribution is the analytical framework itself, demonstrating that transfer-energy constraints, latency variance reduction, and thermodynamic stability jointly define hardware feasibility boundaries. Within the broader ScholarMaster ecosystem, this subsystem functions exclusively to model sustained hardware operating constraints and characterize transfer-energy behavior, without coordinating orchestration policy (Paper 9) or performing federated adaptation (Paper 13).

% ==========================================
% REFERENCES
% ==========================================
\begin{thebibliography}{50}

\bibitem{b_smartgrid_2016} S. Suresh Kumar and C. Sivapragash, ``Time Orient Traffic Estimation Approach to Improve Performance of Smart Grids,'' \textit{Journal of Computational and Theoretical Nanoscience}, vol. 13, no. 8, pp. 5037--5045, 2016.

\bibitem{b_ipfc_2013} B. Gopinath, S. SureshKumar, and M. Ramya, ``Genetically optimized IPFC for improving transient stability performance in power systems,'' in \textit{International Conference Circuits, Power and Computing Technologies (ICCPCT)}, 2013.

\bibitem{b17} H. Sutter, ``The Free Lunch Is Over: A Fundamental Turn Toward Concurrency in Software,'' \textit{Dr. Dobb's Journal}, vol. 30, no. 3, pp. 202--210, 2005.

\bibitem{b_roofline} S. Williams, A. Waterman, and D. Patterson, ``Roofline: An insightful visual performance model for multicore architectures,'' \textit{Communications of the ACM}, vol. 52, no. 4, pp. 65--76, 2009.

\bibitem{b16} G. E. Moore, ``Cramming more components onto integrated circuits,'' \textit{Electronics}, vol. 38, no. 8, pp. 114--117, 1965.

\bibitem{b11} J. Dean, ``The Deep Learning Revolution and Its Implications for Computer Architecture and Chip Design,'' in \textit{IEEE International Solid-State Circuits Conference (ISSCC) Digest of Technical Papers}, 2020, pp. 8--14.

\bibitem{b1} V. Sze, Y.-H. Chen, T.-J. Yang, and J. S. Emer, ``Efficient processing of deep neural networks: A tutorial and survey,'' \textit{Proceedings of the IEEE}, vol. 105, no. 12, pp. 2295--2329, 2017.

\bibitem{b_tpu} N. P. Jouppi et al., ``In-Datacenter Performance Analysis of a Tensor Processing Unit,'' in \textit{Proc. 44th Annual International Symposium on Computer Architecture (ISCA)}, 2017, pp. 1--12.

\bibitem{b_eyeriss} Y.-H. Chen, T. Krishna, J. S. Emer, and V. Sze, ``Eyeriss: An Energy-Efficient Reconfigurable Accelerator for Deep Convolutional Neural Networks,'' \textit{IEEE Journal of Solid-State Circuits}, vol. 52, no. 1, pp. 127--138, 2017.

\bibitem{b_eyeriss_v2} Y.-H. Chen, T.-J. Yang, J. Emer, and V. Sze, ``Eyeriss v2: A flexible accelerator for emerging deep neural networks on mobile devices,'' \textit{IEEE Journal on Emerging and Selected Topics in Circuits and Systems}, vol. 9, no. 2, pp. 292--308, 2019.

\bibitem{b31} M. Sandler, A. Howard, M. Zhu, A. Zhmoginov, and L.-C. Chen, ``MobileNetV2: Inverted residuals and linear bottlenecks,'' in \textit{Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition (CVPR)}, 2018, pp. 4510--4520.

\bibitem{b35} F. N. Iandola, S. Han, M. W. Moskewicz, K. Ashraf, W. J. Dally, and K. Keutzer, ``SqueezeNet: AlexNet-level accuracy with 50x fewer parameters and <0.5MB model size,'' \textit{arXiv preprint arXiv:1602.07360}, 2016.

\bibitem{b20} D. Abts et al., ``Think fast: A tensor streaming processor (TSP) for accelerating deep learning workloads,'' in \textit{Proceedings of the ACM/IEEE 47th Annual International Symposium on Computer Architecture (ISCA)}, 2020, pp. 145--158.

\bibitem{b8} M. Horowitz, ``1.1 Computing's energy problem (and what we can do about it),'' in \textit{IEEE International Solid-State Circuits Conference (ISSCC) Digest of Technical Papers}, 2014, pp. 10--14.

\bibitem{b2} J. L. Hennessy and D. A. Patterson, \textit{Computer Architecture: A Quantitative Approach}, 6th ed. Cambridge, MA: Morgan Kaufmann, 2017.

\bibitem{b15} T. Mudge, ``Power: A first-class architectural design constraint,'' \textit{Computer}, vol. 34, no. 4, pp. 52--58, 2001.

\bibitem{b12} D. Patterson et al., ``Carbon emissions and large neural network training,'' \textit{arXiv preprint arXiv:2104.10350}, 2021.

\bibitem{b_energy_dl} E. Strubell, A. Ganesh, and A. McCallum, ``Energy and policy considerations for deep learning in NLP,'' in \textit{Proceedings of the 57th Annual Meeting of the Association for Computational Linguistics (ACL)}, 2019, pp. 3645--3650.

\bibitem{b_thermal} K. Skadron et al., ``Temperature-aware microarchitecture,'' in \textit{Proc. 30th Annual International Symposium on Computer Architecture (ISCA)}, 2003, pp. 2--13.

% --- ADDED REFERENCES ---

\bibitem{b5} H. Esmaeilzadeh, E. Blem, R. St. Amant, K. Sankaralingam, and D. Burger, ``Dark silicon and the end of multicore scaling,'' in \textit{Proc. 38th Annual International Symposium on Computer Architecture (ISCA)}, 2011, pp. 365--376.

\bibitem{b18} J. Shalf, ``The future of computing beyond Moore's Law,'' \textit{Philosophical Transactions of the Royal Society A}, vol. 378, no. 2166, p. 20190061, 2020.

\bibitem{b24} M. Rhu, N. Gimelshein, J. Clemons, A. Zulfiqar, and S. W. Keckler, ``vDNN: Virtualized Deep Neural Networks for Scalable, Memory-Efficient Neural Network Design,'' in \textit{Proc. 49th Annual IEEE/ACM International Symposium on Microarchitecture (MICRO)}, 2016, pp. 1--13.

\bibitem{b19} R. Kumar, K. I. Farkas, N. P. Jouppi, P. Ranganathan, and D. M. Tullsen, ``Single-ISA Heterogeneous Multi-Core Architectures,'' in \textit{Proc. 36th Annual IEEE/ACM International Symposium on Microarchitecture (MICRO)}, 2003, pp. 81--92.

\bibitem{b21} C.-J. Wu et al., ``Machine Learning at Facebook: Understanding Inference at the Edge,'' in \textit{Proc. IEEE International Symposium on High Performance Computer Architecture (HPCA)}, 2019, pp. 331--344.

\bibitem{b37} B. Jacob et al., ``Quantization and Training of Neural Networks for Efficient Integer-Arithmetic-Only Inference,'' in \textit{Proc. IEEE Conference on Computer Vision and Pattern Recognition (CVPR)}, 2018, pp. 2704--2713.

\bibitem{b38} A. Gholami, S. Kim, Z. Dong, Z. Yao, M. W. Mahoney, and K. Keutzer, ``A Survey of Quantization Methods for Efficient Neural Network Inference,'' \textit{arXiv preprint arXiv:2103.13630}, 2021.

\end{thebibliography}

\end{document}
